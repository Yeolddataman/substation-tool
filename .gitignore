# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — NEVER commit (contains secrets: password hash, JWT secret, API key)
.env
.env.local
.env.*.local

# Raw source data — too large for git, not redistributable
manual_data/

# One-off extract outputs — contain live operational data, not for repo
scripts/nerda-extract-results-*.json
scripts/nerda-extract-summary-*.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
